from datetime import datetime
import os

def agendar_cita(fecha, hora):
    # Simplificado: imprime en consola (para demo)
    print(f"Cita agendada: {fecha} a las {hora}")
    # Aquí iría la integración real con Google Calendar API
